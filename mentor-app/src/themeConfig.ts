/**
 * Ant Design v5 theme — re-export from theme adapter.
 * Token values live in theme/tokens.ts; adapter maps them to ThemeConfig (no raw values in adapter).
 * Usage: <ConfigProvider theme={themeConfig}> ... </ConfigProvider>
 */

import { themeConfig } from './theme/antThemeAdapter';

export { themeConfig };
export default themeConfig;
